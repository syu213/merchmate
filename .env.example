# Environment Configuration Example
# Copy this file to .env.local and fill in your actual values

# ⚠️  SECURITY WARNING ⚠️
# NEVER commit your .env.local file to version control
# NEVER expose API keys in frontend code
# ALWAYS keep API keys on the server-side only

# Google Gemini API Key (Server-side only)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Development server port
PORT=3001

# ⚠️  DO NOT USE VITE_GEMINI_API_KEY IN PRODUCTION ⚠️
# This is only for development testing and is INSECURE
# The secure approach uses GEMINI_API_KEY on the server
# VITE_GEMINI_API_KEY=your_insecure_frontend_api_key